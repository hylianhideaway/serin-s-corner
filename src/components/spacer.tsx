import * as React from "react"


/**
 * React element used to add horizontal spacing between sections.
 * Used throughout the website for uniform spacing needs. 
 * @returns 
 */
const SpacerDiv: React.FC = () => {

    return (
      <div style={{height:"15px"}}/>
    )
  }

export default SpacerDiv
